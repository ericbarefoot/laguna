# Simulation

`FlumeLab(..., simulate=True)` rehearses a whole experiment with no hardware
attached — the scheduler, clock, event log, and safety verbs are all real,
only the wire is faked. See the [simulation guide](../guides/simulate.md)
for usage and what is/isn't simulated.

::: laguna.simulation
