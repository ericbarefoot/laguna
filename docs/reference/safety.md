# Safety

One pause/stop/estop/resume vocabulary, implemented identically by every
subsystem — see the module docstring below for what each verb means and why
the unification happened.

::: laguna.safety
