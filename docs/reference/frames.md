# Reference frames

Puts every instrument's data into one coordinate system, so "the same spot"
means one experiment coordinate instead of a different gantry position per
instrument. See the [subsystem guide](../subsystems/frames.md) for usage.

::: laguna.frames
