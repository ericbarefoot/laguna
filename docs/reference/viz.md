# Visualization

Quick-look plots for scans, profiles, and planned trajectories —
`plot_acquisition()` for acquired data (a `SurfaceScan`, `ProfileResult`, or
an `orient_profile()` DataFrame), `plot_trajectory()` for a planned
`Survey`/`Pass` list. Optional dependency: `pip install 'laguna[viz]'`.

::: laguna.viz
