# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# C extensions
*.so

# Distribution / packaging
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
pip-wheel-metadata/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# PyInstaller
*.manifest
*.spec

# Installer logs
pip-log.txt
pip-delete-this-directory.txt

# Unit test / coverage reports
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
*.py,cover
.hypothesis/
.pytest_cache/

# Virtual environments
venv/
ENV/
env/
.venv

# IDEs
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
._*

# Project specific
config/local_config.yaml
config/config_line_scan.yaml
data/raw/
data/processed/
# Gocator scans — a single full-resolution surface is ~30 MB as .npz and
# ~1.2 GB as .csv, so these must never reach the repo (see
# GocatorScanner.save_scan / docs/subsystems/scanner.md).
data/scans/
*.npz
*.laz
*.las
*.ply
logs/
*.log

# Sandbox (development scripts, not part of the package)
sandbox/

# Personal/team notes (incident reports, scratch write-ups) — not part of the package
notes/

# Experiments and their data (local only, not part of the package)
experiments/

# Captured images and experiment output
captures/
*.jpg
*.jpeg
*.png
*.tiff
*.tif
*.bmp
*.raw
experiment_checkpoint.json
# ...except documentation figures, which are deliberately committed
!docs/images/*.png
experiment_events.csv
experiment_events_simulated.csv
.claude/

# MkDocs build output (generated via `mkdocs build`, see mkdocs.yml)
site/

# Jupyter autosave checkpoints
.ipynb_checkpoints/
